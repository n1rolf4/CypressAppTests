 # behat.yml
default:
  extensions:
    Behat\MinkExtension:
      default_session: selenium2
      javascript_session: selenium2
      selenium2:
          browser: 'chrome'
          capabilities:
              version: ~
              marionette: null
              chrome:
                switches:
                  - "start-maximized"